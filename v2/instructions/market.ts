import { Signer } from "ethers";
import { PTMarket } from "../typechain-types";
import { NFTVoucherStruct } from "../typechain-types/contracts/core/PTCollection";

export async function selfTrade(ptMarket: PTMarket, user: Signer, voucher: NFTVoucherStruct) {
  
}
